services:
  - type: web
    name: streaming-platform
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run dev:all
    envVars:
      - key: NODE_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 10000
